name: Deploy MeetAndNote

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        run: |
          cd $GITHUB_WORKSPACE

          docker compose down

          docker compose build

          docker compose up -d

          docker image prune -f